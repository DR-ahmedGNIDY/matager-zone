# Suppress peer dependency warnings for optional deps (like nodemailer in next-auth)
# Remove this line if you want strict peer dep checking
legacy-peer-deps=false
# Force exact versions for auth packages
save-exact=false
